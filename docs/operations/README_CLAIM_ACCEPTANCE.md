# README Claim Acceptance Matrix

This matrix is the active Standard v3 plan for making the repository satisfy the minimum accepted requirements implied by its README. A claim is accepted only when implementation, tests, docs, and operational evidence agree.

## Acceptance Summary

Status: `in progress`

Current conclusion: the repository now passes the reproducible local Standard v3 deployment verifier, including a source/docs/examples/web claim-marker gate, hosted CI, CodeQL, release workflow, GHCR publishing, published-image pull, and local published-image rollback/log-capture smoke. It is still not accepted for full target-host deployment under Standard v3 because target-host rollback/log evidence, production secret rotation evidence, host network policy evidence, and real platform backend evidence remain missing.

## Claim Matrix

| README claim | Current status | Evidence now present | Remaining acceptance work |
| --- | --- | --- | --- |
| Complete deployment-accepted ecosystem | Partial | `python scripts\verify_production_deployment.py --thirsty-lang-path "T:\00-Active\thirsty_lang_exploration_0754"` passes; CodeQL run `29226577880` passed for commit `a83c9dc940d40409a5c9531864b07521d735b13b`; release workflow run `29226584539` passed; release `v1.0.3` published; GHCR image `ghcr.io/iamsothirsty/thirstys-waterfall:1.0.3` has digest `sha256:9bcb45941b19bd8ae1b848c5ffecaca8df9a15472ca02efb45999e283fe564bc`; published image pull and local rollback/log smoke pass; `scripts\collect_target_deployment_evidence.py` creates target evidence bundles; `scripts\verify_target_deployment_evidence.py` validates target evidence manifests and requires shared revocation-store proof; `scripts\verify_production_deployment.py --require-target-evidence --target-evidence-manifest <manifest>` now fails closed when target evidence is required; orchestrator status now reports `deployment_accepted: False` until evidence is complete | Execute target deployment, target rollback, secret rotation, shared revocation-store proof, host network policy, and real platform backend evidence capture; attach a manifest that passes the production verifier target-evidence gate |
| Built-in VPN | Partial | VPN backend modules and tests exist; subprocess execution paths are shell-free and command-resolved; web VPN connect/disconnect endpoints no longer return fabricated success payloads; `VPNManager` now connects through backend instances and fails closed instead of returning synthetic endpoints | Prove real OS integration on supported platforms, document privilege requirements, and separate mocked/backend-availability tests from real integration tests |
| 8 firewall types | Partial | Firewall modules and backend factory tests exist; backend command paths are shell-free and command-resolved; packet-filtering uses standard-library IPv4/IPv6 exact and CIDR matching; nftables removal now deletes OS rules by discovered handles; web firewall list no longer reports static active firewalls when backend status is unavailable | Prove each firewall backend applies and rolls back real rules on its supported OS, or clearly mark unsupported modes |
| Privacy-first incognito browser | Partial | Added native document/parser/fetcher/layout engine layer and compatibility tests; local network navigation is covered with a deterministic HTTP fixture; ephemeral tab session snapshots are covered without retaining incognito history; web UI no longer auto-logs in with demo credentials or displays fake rendered-page VPN/encryption claims; browser tab API now uses the active browser runtime and fails closed when inactive; browser status now separates encrypted helper activity from accepted end-to-end browser encryption/native-engine claims; encrypted search no longer returns placeholder results when no backend is configured; local encrypted navigation history search backend now returns encrypted session-history matches without plaintext result records; unavailable browser-download results now return encrypted URL tokens instead of plaintext URLs; configured local browser download backend stores downloaded bytes as ciphertext | Add broader rendering coverage, define supported-site acceptance evidence, and prove configured encrypted-search/download lifecycle behavior on target hosts |
| Total encryption of all data | Partial | Crypto modules and encrypted storage helpers exist; orchestrator and browser status no longer report `everything_encrypted: True` or accepted encryption tier by default; encrypted-network helper now reports explicit payload-only scope and uses deterministic JSON payload encryption instead of Python stringification or unused extra-layer claims; configured local browser downloads persist ciphertext artifacts; privacy auditor stores local audit events as encrypted records; package runtime logs now route through encrypted handlers with no plaintext stdout emission in local tests, and encrypted log file persistence is covered by plaintext-leak/round-trip evidence; post-quantum encryption facade now fails closed without a configured backend instead of substituting classical AES/Scrypt; `thirstys_waterfall.get_encryption_evidence_report()` and `docs/operations/ENCRYPTION_EVIDENCE_MAP.md` classify covered and uncovered data surfaces while reporting `standard_v3_accepted: false` | End-to-end proof for stored state, browser data, telemetry outside the privacy auditor, target download lifecycle, transport paths, target production logs, and any accepted post-quantum backend |
| No stubs, placeholders, or TODOs | Partial | Deployment verifier now includes a source/docs/examples/web claim-marker gate for accepted-looking terms; full Python marker scan now passes with no matches for claim-stub terms; web static demo-login/fake-active status paths are covered by regression tests; web browser-tab placeholder state was replaced with active-runtime evidence/fail-closed handling; DOS/MFA/network-stealth docs and MFA/DOS/privacy-ledger examples now use evidence-gated/example-only wording; encrypted search placeholder results were replaced with an encrypted fail-closed unavailable response; encrypted navigation history search now fails closed without a configured backend; local AI and local inference no longer report accepted encryption/model claims without evidence; settings/media/remote/consigliere status now reports local helper encryption and unaccepted backend evidence instead of accepted encryption-tier claims; remote browser, remote desktop, secure tunnel, browser download, media downloader, global traffic-block, VPN kill-switch traffic-control, browser sandbox policy/resource/script execution, VPN DNS/IPv6 leak-protection, advanced-stealth transport/node/fronting, privacy-auditor leak-check, hardware/cloud firewall packet-protection, encrypted-network payload parsing/layer claims, post-quantum encryption substitute paths, privacy-risk ML/hardening substitute paths, WiFi deauth/evil-twin/802.11r substitute paths, WiFi controller scan/connect/channel substitute paths, mesh interface/peer-discovery/routing substitute paths, MFA FIDO2/biometric/provisioning substitute language, microVM boot/network/control/metrics substitute paths, hardware-root TPM/Secure-Enclave/HSM substitute paths, and DOS trap Windows syscall/HSM-destruction substitute paths no longer report simulated success without configured evidence | Keep extending claim scans to any newly added source surface; replace or explicitly reclassify remaining production-substitute paths that require real backend evidence |
| Real backends | Partial | Backend factories exist; some tests exercise backend availability; local Docker web backend starts and serves health/auth; IKEv2 availability now requires a concrete OS command path instead of platform name alone; VPN manager startup now fails closed when configured backends are unavailable or fail to connect | Add real VPN/firewall backend execution evidence and fail-closed handling when required OS tools are absent |
| Comprehensive testing | Partial | Latest direct full local test run passed `python -m pytest -q`: 481 tests passed; latest verifier fast path passed `python scripts\verify_production_deployment.py --skip-docker --skip-tests --thirsty-lang-path "T:\00-Active\thirsty_lang_exploration_0754"`; latest full local verifier ran `python -m pytest -q`: 326 tests passed with no pytest warning summary; hosted PR and release workflows passed Linux, Windows, and macOS on Python 3.10-3.11 | Keep separating availability/unit coverage from real OS backend execution tests |
| CI/CD pipeline | Partial | CI test install path now includes `.[test,web]`; flake8 syntax gate is hard; Bandit/Safety no longer swallow failures; Python matrix is now 3.10-3.11 to match the patched web dependency stack; CodeQL run `29226577880` passed; release Docker smoke tests the loaded release image tag without rebuilding; release workflow run `29226584539` passed; PyPI publish is skipped for workflow-dispatch releases; target evidence manifest collector and validator exist | Add target deployment workflow evidence and require the target evidence manifest gate for acceptance |
| Security audits | Partial | Full-repo Bandit passes with 0 findings; deployment lock vulnerability check passes with 0 vulnerabilities and 0 ignored vulnerabilities; CodeQL run `29226577880` passed on main | Continue scheduled CodeQL monitoring and triage any future findings |
| Public README/marketing/comparison claims | Partial | README high-risk feature, architecture, platform, security, and comparison language now points to current evidence and remaining Standard v3 gaps; `docs/SHOWCASE.md` and `docs/COMPETITION_COMPARISON.md` now describe current evidence instead of claiming accepted production superiority | Keep README and public docs aligned as implementation evidence changes |
| Web API production authentication | Partial | Default hardcoded `admin/admin` login removed; password-hash auth added; fail-closed tests added; production import rejects missing secrets, wildcard CORS, and demo login; local and container production-mode login smoke pass with explicit secrets; logout revokes the presented token JTI through the configured revocation store; SQLite shared revocation store support is covered by tests; session-policy endpoint and operator setup doc now report token lifetimes and revocation-store scope | Add target secret rotation execution evidence and target proof that every worker/container uses the same shared revocation store |
| Thirsty-Lang / sovereign startup integration | Partial | Optional bridge loads local enhanced `utf.tarl` from `T:\00-Active\thirsty_lang_exploration_0754`; local and container health smoke report backend `thirsty-lang` when mounted | Define full governance contract, policy source, denial handling, proof persistence, and CI-safe integration strategy |
| Docker deployment | Partial | Verifier builds `thirstys-waterfall:codex-verify`; release workflow run `29226584539` loads, smoke-tests, and pushes `ghcr.io/iamsothirsty/thirstys-waterfall:1.0.3`; published digest is `sha256:9bcb45941b19bd8ae1b848c5ffecaca8df9a15472ca02efb45999e283fe564bc`; local pull plus health/auth/log and rollback smoke passed with enhanced Thirsty-Lang mounted read-only | Target rollback, non-local secret injection, target logs, production host/network policy, and orchestrator/service hardening evidence |
| Cross-platform support | Partial | CI matrix declares Linux, Windows, and macOS on Python 3.10-3.11; `thirstys_waterfall.get_platform_capabilities()` and `docs/operations/PLATFORM_CAPABILITIES.md` document current VPN/firewall/service capability differences while reporting `production_accepted: false` | Prove platform-specific install/runtime behavior, privilege requirements, and real VPN/firewall apply/rollback on each supported OS |

## Immediate Completion Plan

1. Convert remaining simulated implementation paths into real backends or adjust the README claim language.
2. Execute rollback and secret rotation procedure against the target deployment and capture target logs.
3. Attach target-host deployment, rollback, secret-rotation, and log evidence to this matrix.
4. Prove real OS VPN/firewall backend execution or narrow the related README claims further.

## Evidence Required Before Marking Complete

- Clean full local test run.
- Clean package build and install from wheel.
- Clean import/startup path without local-only dependencies.
- Explicit Thirsty-Lang optional-binding behavior with and without local checkout.
- Clean full-repo Bandit scan.
- Clean locked dependency vulnerability scan.
- Docker build and container smoke evidence.
- Reproducible verifier evidence from `scripts/verify_production_deployment.py`.
- Reproducible target evidence verification from `scripts\verify_production_deployment.py --require-target-evidence --target-evidence-manifest <manifest>`.
- CI run showing hard test/security/syntax gates.
- Release run showing artifact build, Docker smoke, GHCR push, and GitHub release creation.
- Published image pull and smoke evidence.
- Rollback procedure, including local rollback smoke and target-host rollback evidence.
- Secrets review confirming no hardcoded production credentials and production startup rejection for missing/unsafe secrets.
- Continuity map updated with final status.
